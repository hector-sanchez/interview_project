<h1>Get your population numbers here</h1>
<form method="get" action="/populations">
  <input name="year" />
  <button type="submit">Submit</button>
</form>

<% if @population %>
  <%= render partial: "population_result", locals: { year: @year, population: @population } %>
<% end %>